name: Legacy tracker patch (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: canonical tracker patch is handled by build-android.yml"
